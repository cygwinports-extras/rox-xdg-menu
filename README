rox-xdg-menu
------------------------------------------
ROX is a fast, user friendly desktop based on RISC OS which makes 
extensive use of drag-and-drop. The interface revolves around the file 
manager, or filer, following the traditional Unix view that `everything 
is a file' rather than trying to hide the filesystem beneath start 
menus, wizards, or druids.

This package provides a Freedesktop.org Menu Spec application menu.

Runtime requirements:
  python-2.5.4-10
  python-dbus-0.83.0-10
  python-gobject2.0-2.18.0-1
  python-gtk2.0-2.16.0-1
  python-xdg-0.17-1
  rox-lib2-2.0.6-1

Build requirements:
  cygport-0.9.10-1

Canonical website:
  http://xdg-menu.sourceforge.net/

Canonical download:
  http://downloads.sourceforge.net/xdg-menu/

-------------------------------------------

Build instructions:
  unpack rox-xdg-menu-1.4-X-src.tar.bz2
    if you use setup to install this src package, it will be
	 unpacked under /usr/src automatically
  cd /usr/src
  cygport ./rox-xdg-menu-1.4-X.cygport all

This will create:
  /usr/src/rox-xdg-menu-1.4-X-src.tar.bz2
  /usr/src/rox-xdg-menu-1.4-X.tar.bz2

For a list of files included in the binary package, run from the 
command-line: 'cygcheck -cd rox-xdg-menu'.

-------------------------------------------

Port Notes:

----- version 1.4-1 -----
Initial release for Cygwin 1.7.


Cygwin port maintained by:
http://sourceware.org/cygwinports/
